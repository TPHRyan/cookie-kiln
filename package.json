{
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach run build"
  },
  "devDependencies": {
    "prettier": "^2.3.2",
    "tslib": "^2.3.1",
    "typescript": "^4.3.5"
  },
  "packageManager": "yarn@3.0.2"
}
